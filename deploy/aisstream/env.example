# Environment for aisstream-collector.service
# Install to: /etc/aisstream.env  (mode 600, owned by root)
#
# Get a free key: https://aisstream.io/  (sign in with GitHub → API Keys)

AISSTREAM_API_KEY=REPLACE_ME
